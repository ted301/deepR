name: deepR-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - typer>=0.12.0
      - pydantic>=2.5.0
      - rich>=13.0.0
      - pytest
      - pytest-asyncio
